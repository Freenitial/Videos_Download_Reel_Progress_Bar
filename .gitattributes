# Normalize text files; pin Windows-native endings where it matters.
* text=auto

# cmd is the only interpreter that can be picky about line endings: pin CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf

*.js   text
*.json text
*.md   text

# Binary assets
*.png binary
*.crx binary
